<!DOCTYPE html>
<html>
  <head>
    {{>head }}    
  </head>
  <body class="overlay">
    {{{ body }}}
    {{#if env }}{{#if debug }}
    {{/if }}{{/if }}  
    <footer class="container-fluid">
      {{>footer }}
    </footer>
  
  </body>
</html>
